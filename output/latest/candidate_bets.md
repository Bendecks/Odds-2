# Candidate Bets

Forward-eligible only. Historical proxy rows are excluded.

Source used: prediction_snapshots_latest_forward_only

No forward-eligible rows. Historical proxy rows are excluded from candidate bets.

Rejected signals: 0