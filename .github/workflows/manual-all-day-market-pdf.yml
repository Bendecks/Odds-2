name: Manual All-Day Market PDF

on:
  workflow_dispatch:
    inputs:
      sports:
        description: 'Comma-separated Odds-API sport slugs'
        required: true
        default: 'football,tennis,basketball,ice-hockey,american-football,baseball,cricket,rugby-union,rugby-league,handball,esports'
        type: string
      max_price_events_per_sport:
        description: 'Max whole-day events per sport to request markets for'
        required: true
        default: '200'
        type: string
      max_pages:
        description: 'Max /events pages per sport for whole-day coverage'
        required: true
        default: '10'
        type: string
      min_bookmaker_count:
        description: 'Fallback coverage filter for sports/leagues not on allowlist'
        required: true
        default: '10'
        type: string
  schedule:
    - cron: '0 */4 * * *'

jobs:
  build-all-day-market-pdf:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    env:
      ODDS_API_IO_KEY: ${{ secrets.ODDS_API_IO_KEY }}
      BET365_ALL_DAY_SPORTS: ${{ inputs.sports || 'football,tennis,basketball,ice-hockey,american-football,baseball,cricket,rugby-union,rugby-league,handball,esports' }}
      BET365_ALL_DAY_MAX_PRICE_EVENTS_PER_SPORT: ${{ inputs.max_price_events_per_sport || '200' }}
      BET365_ALL_DAY_MAX_PAGES: ${{ inputs.max_pages || '10' }}
      BET365_ALL_DAY_MIN_BOOKMAKER_COUNT: ${{ inputs.min_bookmaker_count || '10' }}
      BET365_ALL_DAY_MAX_EVENTS: '100'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install reportlab

      - name: Build all-day market PDF
        run: |
          python scripts/build_bet365_today_multisport_all_day_pdf.py \
            --sports "${BET365_ALL_DAY_SPORTS}" \
            --max-price-events-per-sport "${BET365_ALL_DAY_MAX_PRICE_EVENTS_PER_SPORT}" \
            --max-pages "${BET365_ALL_DAY_MAX_PAGES}" \
            --min-bookmaker-count "${BET365_ALL_DAY_MIN_BOOKMAKER_COUNT}"

      - name: Create compressed AI data bundle
        run: |
          mkdir -p output/bet365/latest/email_bundle
          cp output/bet365/latest/bet365_today_multisport_all_day_markets.jsonl output/bet365/latest/email_bundle/ || true
          cp output/bet365/latest/bet365_today_multisport_all_day_events.json output/bet365/latest/email_bundle/ || true
          cp output/bet365/latest/bet365_today_multisport_all_day_summary.json output/bet365/latest/email_bundle/ || true
          cp output/bet365/latest/bet365_today_multisport_all_day_markets.csv output/bet365/latest/email_bundle/ || true
          cp output/bet365/latest/bet365_today_multisport_all_day_events.csv output/bet365/latest/email_bundle/ || true
          cp output/bet365/latest/bet365_today_multisport_all_day_excluded.csv output/bet365/latest/email_bundle/ || true
          cp output/bet365/latest/bet365_today_multisport_all_day_rate_limit_headers.csv output/bet365/latest/email_bundle/ || true
          cd output/bet365/latest/email_bundle
          zip -9 ../bet365_next_12h_ai_data.zip ./*
          cd -
          ls -lh output/bet365/latest/bet365_next_12h_ai_data.zip

      - name: Commit all-day market files
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add output/bet365/latest/bet365_today_multisport_all_day_report.pdf
          git add output/bet365/latest/bet365_today_multisport_all_day_events.csv
          git add output/bet365/latest/bet365_today_multisport_all_day_markets.csv
          git add output/bet365/latest/bet365_today_multisport_all_day_events.json
          git add output/bet365/latest/bet365_today_multisport_all_day_markets.jsonl
          git add output/bet365/latest/bet365_today_multisport_all_day_excluded.csv
          git add output/bet365/latest/bet365_today_multisport_all_day_summary.json
          git add output/bet365/latest/bet365_today_multisport_all_day_rate_limit_headers.csv
          git add output/bet365/latest/bet365_next_12h_ai_data.zip
          git add data/raw/odds_api_io/bet365_today_multisport_all_day_report
          if git diff --cached --quiet; then
            echo "No all-day PDF changes to commit"
          else
            git commit -m "Update all-day market AI exports"
            git pull --rebase origin main
            git push
          fi

      - name: Upload all-day AI data artifact
        uses: actions/upload-artifact@v4
        with:
          name: bet365-next-12h-ai-data
          path: |
            output/bet365/latest/bet365_next_12h_ai_data.zip
            output/bet365/latest/bet365_today_multisport_all_day_markets.jsonl
            output/bet365/latest/bet365_today_multisport_all_day_events.json
            output/bet365/latest/bet365_today_multisport_all_day_summary.json
            output/bet365/latest/bet365_today_multisport_all_day_markets.csv
            output/bet365/latest/bet365_today_multisport_all_day_events.csv
            output/bet365/latest/bet365_today_multisport_all_day_excluded.csv
            output/bet365/latest/bet365_today_multisport_all_day_rate_limit_headers.csv
            output/bet365/latest/bet365_today_multisport_all_day_report.pdf

      - name: Email compressed AI-native Bet365 data
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.SMTP_HOST }}
          server_port: ${{ secrets.SMTP_PORT }}
          username: ${{ secrets.SMTP_USERNAME }}
          password: ${{ secrets.SMTP_PASSWORD }}
          subject: 'Bet365 next 12h AI-native market data'
          to: ${{ secrets.REPORT_EMAIL_TO }}
          from: ${{ secrets.REPORT_EMAIL_FROM }}
          secure: false
          body: |
            AI-native Bet365 data for the next 12 hours is attached as a compressed ZIP.

            Inside the ZIP:
            - bet365_today_multisport_all_day_markets.jsonl  (primary AI file)
            - bet365_today_multisport_all_day_events.json    (nested event file)
            - bet365_today_multisport_all_day_summary.json   (metadata)
            - CSV backup files

            PDF is generated in the repo/artifact for optional human review, but not emailed to avoid Gmail size limits.
          attachments: output/bet365/latest/bet365_next_12h_ai_data.zip
